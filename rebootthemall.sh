#!/bin/bash

ssh pi@camera-1.local sudo reboot now
ssh pi@camera-2.local sudo reboot now
ssh pi@camera-3.local sudo reboot now
ssh pi@camera-4.local sudo reboot now
ssh pi@camera-5.local sudo reboot now
ssh pi@camera-6.local sudo reboot now

